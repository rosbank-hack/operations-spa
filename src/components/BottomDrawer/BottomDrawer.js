import React from 'react';

import useStyles from './styles';

const BottomDrawer = () => {
  const classes = useStyles();

  return <p className={classes.root}>Bottom Drawer</p>;
};

export default BottomDrawer;
